/* home.js — intentionally empty.
   Round 2 (his verdict: "too gimmicky ... do assets properly"): the
   pointer-parallax constellation was removed. The hero now rests on the
   author's own gold-on-black montage (a real artifact), composed static.
   No canvas, no drift loop, no pointer wiring. Kept as a stub so no build
   step re-references a deleted file. */
